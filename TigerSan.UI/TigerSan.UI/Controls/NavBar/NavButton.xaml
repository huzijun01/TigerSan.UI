<UserControl x:Class="TigerSan.UI.Controls.NavButton"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:TigerSan.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="200"
             Cursor="Hand">
    <UserControl.Resources>
        <ResourceDictionary Source="/TigerSan.UI;component/Controls/NavBar/NavBarStyle.xaml"/>
    </UserControl.Resources>

    <Border
        Padding="10"
        CornerRadius="8"
        Background="{Binding Background}"
        d:DataContext="{StaticResource NavButtonDesignData}"
        DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 图标: -->
            <Label
                x:Name="icon"
                Grid.Column="0"
                Content="{Binding Icon}"
                Foreground="{Binding Foreground}"
                Style="{StaticResource NavLabelStyle}"/>

            <!-- 文本: -->
            <TextBlock
                x:Name="title"
                Grid.Column="1"
                Text="{Binding Title}"
                FontWeight="{Binding FontWeight}"
                Foreground="{Binding Foreground}"
                Style="{StaticResource NavTextBlockStyle}"/>
        </Grid>
    </Border>
</UserControl>
