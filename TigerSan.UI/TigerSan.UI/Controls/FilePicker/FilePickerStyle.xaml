<ResourceDictionary
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:TigerSan.UI.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/TigerSan.UI;component/Styles/Global/Global.xaml"/>
        <ResourceDictionary>
            <!-- 文件选择器： -->
            <Style
                x:Key="FilePickerStyle"
                TargetType="{x:Type local:FilePicker}"
                BasedOn="{StaticResource TransparentUserControlStyle}">
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="AllowDrop" Value="True" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="{StaticResource Button.Padding}" />
                <Setter Property="Foreground" Value="{StaticResource Colors.Brand}" />
                <Setter Property="BorderBrush" Value="{StaticResource Colors.Brand}" />
            </Style>

            <!-- 文件选择器（全局）： -->
            <Style TargetType="{x:Type local:FilePicker}" BasedOn="{StaticResource FilePickerStyle}"/>

            <!-- 设计数据： -->
            <local:FilePickerDesignData x:Key="FilePickerDesignData"/>
        </ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>
</ResourceDictionary>