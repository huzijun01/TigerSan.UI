<UserControl x:Class="TigerSan.UI.Controls.NumBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:TigerSan.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="30"
             d:DesignWidth="200">

    <UserControl.Resources>
        <ResourceDictionary Source="./NumBoxStyle.xaml" />
    </UserControl.Resources>

    <Border
        Width="{Binding Width}"
        Height="{Binding Height}"
        Background="{Binding Background}"
        BorderBrush="{Binding BorderBrush}"
        BorderThickness="{Binding BorderThickness}"
        CornerRadius="{StaticResource TextBox.CornerRadius}"
        d:DataContext="{StaticResource NumBoxDesignData}"
        DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 文本框： -->
            <TextBox
                x:Name="content"
                IsEnabled="{Binding IsEnable}"
                Style="{StaticResource NumBoxTextBoxStyle}"/>

            <!-- 按钮： -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button
                    x:Name="btnUp"
                    Content="▲"
                    IsEnabled="{Binding IsEnable}"
                    Command="{Binding btnUp_ClickCommand}"
                    Style="{StaticResource NumBoxButtonStyle}"/>

                <Button
                    x:Name="btnDown"
                    Grid.Row="1"
                    Content="▼"
                    IsEnabled="{Binding IsEnable}"
                    Command="{Binding btnDown_ClickCommand}"
                    Style="{StaticResource NumBoxButtonStyle}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
