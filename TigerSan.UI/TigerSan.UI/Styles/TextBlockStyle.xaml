<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--  标题：  -->
    <Style x:Key="TitleTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Margin" Value="0,20" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource Colors.PrimaryText}" />
    </Style>

    <!--  内容：  -->
    <Style x:Key="ContentTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!--  属性名：  -->
    <Style x:Key="PropNameTextBlockStyle"
           TargetType="{x:Type TextBlock}"
           BasedOn="{StaticResource ContentTextBlockStyle}">
        <Setter Property="Foreground" Value="{DynamicResource Colors.RegularText}" />
    </Style>

    <!--  属性值：  -->
    <Style x:Key="PropValueTextBlockStyle"
           TargetType="{x:Type TextBlock}"
           BasedOn="{StaticResource ContentTextBlockStyle}">
        <Setter Property="Foreground" Value="{DynamicResource Colors.Brand}" />
    </Style>

    <!--  链接：  -->
    <Style x:Key="UrlTextBlockStyle"
           TargetType="{x:Type TextBlock}"
           BasedOn="{StaticResource ContentTextBlockStyle}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="TextDecorations" Value="Underline" />
        <Setter Property="Foreground" Value="{DynamicResource Colors.Brand}" />
    </Style>
</ResourceDictionary>