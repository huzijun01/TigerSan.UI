<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/TigerSan.UI;component/Styles/Global/Global.xaml"/>
        <ResourceDictionary Source="/TigerSan.UI;component/Styles/TextBoxTemplate.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- 自定义文本框样式： -->
    <Style x:Key="CustomTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Padding" Value="{StaticResource TextBox.Padding}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Colors.DarkBorder}"/>
        <Setter Property="BorderThickness" Value="{StaticResource TextBox.BorderThickness}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Colors.BasicWhite}"/>
        <Setter Property="Foreground" Value="{StaticResource Colors.PrimaryText}"/>
        <Setter Property="Background" Value="{StaticResource Colors.DarkFill}"/>
        <Setter Property="Template" Value="{StaticResource CustomTextBoxTemplate}"/>
    </Style>

    <!-- 无边框文本框样式：（对话框中会进行内容尺寸计算，请勿设置边距。） -->
    <Style x:Key="NoBorderTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Colors.RegularText}" />
        <Setter Property="CaretBrush" Value="{DynamicResource Colors.RegularText}" />
    </Style>

    <!--  省略号文本框：  -->
    <Style
        x:Key="EllipsisTextBoxStyle"
        TargetType="{x:Type TextBox}"
        BasedOn="{StaticResource NoBorderTextBoxStyle}">
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsReadOnly" Value="True"/>
                    <Condition Property="AcceptsReturn" Value="False"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter Property="Template" Value="{StaticResource EllipsisTextBoxTemplate}"/>
                </MultiTrigger.Setters>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- 默认文本框样式： -->
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource CustomTextBoxStyle}"/>
</ResourceDictionary>